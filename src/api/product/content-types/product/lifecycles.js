'use strict';

module.exports = {
  async beforeCreate(event) {
    // Optional: Add logic before creating a product
  },
  async afterCreate(event) {
    // Optional: Add logic after creating a product
  },
};